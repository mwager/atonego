# AtOneGo rewrite docs

This directory contains the source files of my angular/ionic rewrite. See `/readme.md` for more information on the project.

## Dev workflow

    # watch files
    $ gulp dev
    # other tab
    $ ionic serve
